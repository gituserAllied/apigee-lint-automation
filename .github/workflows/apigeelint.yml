name: ApigeeLint Automation

on:
  push:
    branches:
      - main
    paths:
      - "apiproxy/**"
      - "sharedflows/**"
      - ".github/workflows/apigeelint.yml"

jobs:
  lint-and-report:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install ApigeeLint
        run: npm install -g apigeelint

      - name: Prepare Output Folders
        run: mkdir -p JsonFile ExcelFile

      - name: Detect Bundles to Lint
        id: detect
        run: |
          echo "🔎 Detecting changed or new apiproxy/sharedflow bundles..."
          changed=$(git diff --name-only HEAD^ HEAD | grep -E '^(apiproxy|sharedflows)/' | cut -d/ -f1-2 | sort -u | uniq || true)

          if [ -z "$changed" ]; then
            echo "⚠️ No specific changes found — running ApigeeLint for all bundles."
            all_paths=$(find apiproxy sharedflows -mindepth 1 -maxdepth 1 -type d 2>/dev/null || true)
            echo "$all_paths" > changed.txt
            echo "run_lint=true" >> $GITHUB_ENV
          else
            echo "✅ Found changed bundles:"
            echo "$changed"
            echo "$changed" > changed.txt
            echo "run_lint=true" >> $GITHUB_ENV
          fi

      - name: Run ApigeeLint
        if: env.run_lint == 'true'
        run: |
          echo "🚀 Running ApigeeLint..."
          while read bundle; do
            [ -z "$bundle" ] && continue
            if [ -d "$bundle/apiproxy" ]; then
              echo "🔍 Linting API Proxy: $bundle/apiproxy"
              apigeelint -s "$bundle/apiproxy" -f json.js -w "JsonFile/${bundle//\//_}_proxy_report.json" || true
            elif [ -d "$bundle/sharedflowbundle" ]; then
              echo "🔍 Linting Shared Flow: $bundle/sharedflowbundle"
              apigeelint -s "$bundle/sharedflowbundle" -f json.js -w "JsonFile/${bundle//\//_}_sf_report.json" || true
            else
              echo "⚠️ Skipping $bundle (no valid apiproxy/sharedflowbundle found)"
            fi
          done < changed.txt

      - name: Show JSON Output
        if: env.run_lint == 'true'
        run: |
          echo "✅ Lint completed. JSON reports generated:"
          ls -lh JsonFile || true
          echo ""
          for f in JsonFile/*.json; do
            echo "===== $f ====="
            cat "$f"
            echo ""
          done

      - name: Commit and Push JSON Output
        if: env.run_lint == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add JsonFile/*.json || true
          git commit -m "Auto: Added ApigeeLint reports" || echo "No new JSON to commit"
          git push
